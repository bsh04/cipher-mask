import React from 'react';
import {View} from 'react-native'
import Header from "../components/header";

export const Cipher = () => {
    return (
        <View>
            <Header
                title={'О кодировки'}
            />
        </View>
    );
};